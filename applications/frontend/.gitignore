_build/
target/
.mooncakes/
node_modules/
.sol/
package-lock.json
wrangler.json

# Generated by `sol build` / `sol generate` — regenerated in CI, never hand-edited.
# app/__gen__/types/ is committed so `moon check` in CI can resolve @types imports
# before `sol build` runs. Sol regenerates it on each build (content is stable).
app/__gen__/server/
app/__gen__/client/
app/client/__gen__*
