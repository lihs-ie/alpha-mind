///| Client-side element DSL imports for Island components.
///| Hydration functions are executed in the browser context.
using @element {
  type DomNode,
  div,
  span,
  button,
  input,
  form,
  label,
  h1,
  p,
  text,
  text_of,
  events,
}
