///| Server-side element DSL imports for SSR page handlers.
///| These are server components: no event handlers, SSR only.
using @server_dom {
  a,
  div,
  span,
  button,
  h1,
  h2,
  p,
  form,
  input,
  label,
  text,
}
