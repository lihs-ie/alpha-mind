rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // クライアントからの直接アクセスを禁止。
    // BFF/マイクロサービスはAdmin SDK + IAMでアクセスする。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
